language: c
install:
  - sudo apt-get update -yqq
  - sudo apt-get install mono-devel mono-xbuild mono-tools libgtk2.0-cil-dev intltool -yqq
script:
  - ./autogen.sh && make
after_script:
  - ./gendarme.sh
